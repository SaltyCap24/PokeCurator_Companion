<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Draw both companion tools above Pokemon GO / PGSharp. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Network: fetch cleanup recommendations from the PokeCurator server. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Keep the overlays and screen capture alive via foreground services. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PokeCuratorCompanion">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pokecurator" android:host="overlay" />
            </intent-filter>
        </activity>

        <activity
            android:name=".VisionCaptureActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Translucent.NoTitleBar" />

        <!-- Existing transfer-helper overlay. Kept unchanged and separate. -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating cleanup overlay shown above Pokemon GO during transfers." />
        </service>

        <!-- New OCR-powered full-screen collection grid assistant. -->
        <service
            android:name=".VisionOverlayService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />
    </application>
</manifest>
